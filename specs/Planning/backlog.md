# DreamGenClone — Backlog & Enhancement Ideas

Items here are high-level ideas.  Each item must go through **Design → Plan → Implement** before work begins.

---

## States

| State | Meaning |
|---|---|
| `new` | Idea captured, no design work started |
| `designed` | Design document or notes written, approach agreed |
| `planned` | Tasks broken down, effort estimated, ready to schedule |
| `implemented` | Code complete, awaiting integration test / review |
| `debugging` | Implemented but known defects being worked |
| `done` | Functionally complete and stable in dev |
| `done done` | Verified in production / end-to-end validated, closed |

---

## Priority

| Value | Meaning |
|---|---|
| `high` | Must be addressed soon; blocks key functionality or quality |
| `medium` | Important but not urgent |
| `low` | Nice to have; defer until higher-priority items are done |

## Scope

| Value | Meaning |
|---|---|
| `small` | Contained change — a few files, low risk |
| `medium` | Moderate change — several components, some design needed |
| `large` | Major change — cross-cutting, significant design and test effort |

---

## Backlog

| # | Title | State | Priority | Scope | Notes |
|---|---|---|---|---|---|
| B-001 | Fix and enhance the Question/Decision prompt | `new` | medium | small | Decision prompts are not generating well-formed or contextually appropriate questions; needs redesign and testing |
| B-002 | Fix locations — scene location is incorrect or missing | `new` | medium | small | Location injection into prompts is broken or not resolving correctly for active scenario context |
| B-004 | Test completed single theme run multiple times | `new` | low | small | Ensure repeat-run penalty, score decay, and cooldown logic work correctly across multiple completions of the same theme |
| B-005 | Performance pass | `new` | medium | medium | Profile startup, per-interaction latency, and DB query cost; identify and fix top bottlenecks |
| B-011 | BUG: Reset phase does not restore stats to baseline-adjusted values | `new` | high | small | Stat pull toward baseline on Reset is not applying as designed; actual values after Reset do not match expected decay schedule |
| B-016 | Phase Ladder visualization | `new` | low | small | Show all narrative phases in a ladder/flow view with current-phase highlight and progress-to-next indicator; defined in HP2 enhancements backlog |
| B-017 | Question subsystem — on-demand invocation control | `new` | medium | medium | Allow user-triggered adaptive Q&A generation per character at any time via UI; trigger scope (per-character / current-speaker / both) to be decided |
| B-018 | Phase-aware question steering policy | `new` | medium | small | Make adaptive Q&A generation phase-sensitive; BuildUp should favour theme/context steering over stat-pressure; define phase weighting matrix |
| B-019 | Universal direct-question + scene-location trigger — residual hardening | `new` | medium | small | Slice 1–5 implemented; remaining: end-to-end validation, cadence-bypass edge cases, and HP2 manual spot-check confirmation |
| B-020 | Steer commands — apply stat changes on execution | `new` | high | small | When a Steer command is executed, character stats (Desire, Restraint, Tension, Connection) should be updated to reflect the steering intent; design must define which stats are affected, by how much, and how the steering direction maps to stat deltas |
| B-022 | Character stat profile ladder | `new` | medium | medium | Define a progression ladder for character stat profiles — ordered tiers from reserved/inhibited through to fully uninhibited — so profiles have a clear escalation path and users can understand how a character's stat baseline relates to their narrative disposition |
| B-023 | Update the RP Assistant | `new` | medium | medium | Review and improve the RP Assistant behavior; scope and specific improvements to be defined during design |
| B-030 | Implement the woman willingness profile | `new` | medium | medium | Define and implement the woman willingness profile — a configurable profile that governs how readily a female character consents to or resists escalation in the narrative; includes domain model, persistence, prompt injection, and UI CRUD |
| B-032 | Scene Image Generator Engine | `new` | low | large | Engine that generates images for RP scenes; covers model/provider integration, prompt construction from scene context (characters, setting, intensity), scheduling/queuing, and display in the RP workspace |
| B-036 | Position catalog, matrix picker, and no-repeat tracking | `planned` | high | large | Full feature to manage sexual positions as first-class domain objects and drive them through the RP engine. Phases: (1) `RPPosition` domain class + DB table + seed service with 25 canonical positions; (2) position CRUD service interface + implementation; (3) climax beat/sub-beat matrix rows reference position IDs via FK (migration); (4) position catalog UI + matrix beat picker (replace free-text with position selector); (5) no-repeat tracking in workspace — suppress recently used positions from steer options; (6) AI prompt updates — inject full position list, priority flags, and no-repeat set into continuation prompts; (7) detailed position context injection after a position is selected during a scene. Phases 1–2 implemented; Phase 3 in progress. |
| B-037 | Character profile attributes expansion — personality, background, interests, marital status, etc. | `new` | medium | large | Add non-physical character attributes to Character and Persona templates: Tier 1 (personality traits, turn-ons, turn-offs, occupation, speech style/mannerisms, secrets/hidden desires); Tier 2 (hobbies/interests, background, marital status, sexual experience level, relationship to persona, emotional state at scene start); Tier 3 (body language style, kinks/fetishes, motivation in this scenario). All new fields optional free text or preset+custom. Full detail in `specs/Planning/B-037-character-profile-attributes.md`. |
| B-040 | Scenario role placeholders — runtime character substitution | `new` | medium | medium | Allow scenario text fields (premise, phase guidance, escalation notes, etc.) to contain role placeholders such as `{Wife}`, `{Husband}`, `{Otherman}` that are replaced at prompt-build time with the actual character name and persona assigned to that role in the session; covers placeholder syntax, role-to-character resolution, substitution service, and UI hints in scenario editor fields |
| B-045 | TakeTurns mode — proper design and enforcement | `new` | high | medium | TakeTurns mode is not properly designed or enforced. The data model (TurnState enum, ConsecutiveNpcTurns, TurnTakingThreshold, IsUserTurn signal) exists but the enforcement is missing: `IsContinuationAllowed` returns true for all actors, the overflow-continue multi-actor batch does not gate on `CurrentTurnState`, and pressing Continue after `UserTurn` is set simply resets the state and generates more NPC content. At runtime TakeTurns behaves identically to Spectate. Additionally, the multi-actor overflow-continue generates each character's turn with only a generic "Continue the scene naturally" prompt, which causes each actor to re-narrate the same scene moment from their own POV rather than advancing from where the previous actor left off — producing incoherent loops. Design must cover: (1) what TakeTurns should mean (user writes next, NPCs must wait); (2) enforcement contract — block NPC auto-continue when it is the user's turn; (3) per-actor prompt continuity — each actor in a multi-actor batch must be anchored to the prior actor's output, not independently re-narrate the same moment; (4) Climax-phase anchoring already partially implemented must be extended to all phases; (5) UI signal when it is the user's turn. |
| B-046 | Escalation floor — physical contact anchor in NPC continuation prompt | `new` | high | medium | NPC actors do not anchor to the highest physical contact level already established in the session. Observed pattern: a turn with palm-on-back/spine arc and full waist hold (T6) is followed by a regression to a simple hand hold (T8) because each NPC turn is composed independently without a "floor" constraint. The next actor in the same turn self-corrected (thigh press + fingers laced), confirming the model can produce the right content when given the correct context but is not being anchored. Fix requires: (1) tracking a per-session `PhysicalEscalationAnchor` (a short prose summary of the most intimate physical contact established so far, updated each turn by the semantic engine); (2) injecting that anchor as a soft constraint in the NPC continuation prompt — "Physical contact already established in this encounter: [summary]. Do not produce contact less intimate than this floor"; (3) updating the anchor only upward (never downward except on phase Reset). Related to Active Encounter Mode design (separate session) and per-actor prompt continuity in B-045. |
| B-047 | Add event descriptions to semantic inference prompt | `new` | medium | small | The semantic event inference LLM currently receives only bare kebab-case eventIds (e.g. `["forbidden-touch","near-discovery"]`) with no descriptions. This forces the model to infer meaning from the name alone, causing missed detections and false negatives for subtle or new eventIds. Add a `Description` column to `RPThemeSemanticEventMappings` (persisted in DB, editable in UI on RPThemeDetail.razor), then format the inference prompt to include the description alongside each eventId: `- forbidden-touch: physical contact that crosses boundaries` instead of just a flat JSON array. This improves detection accuracy for all event types, especially user-added custom eventIds whose names may be ambiguous to the model. |
| B-048 | Push scenario character stat changes into running sessions | `new` | medium | medium | Scenario character `BaseStats` edits only seed new sessions — running sessions keep their own `AdaptiveState.CharacterStats` independently and overwrite on each continuation. Need a mechanism to detect scenario stat changes and push them into an already-running session — either an explicit "Apply to session" button in the scenario editor, or an automatic sync on session load that detects scenario changes since the session was created and offers to reconcile. Design must cover: (1) detection of stat drift (scenario stats vs. session BaselineStats), (2) user confirmation flow (push all / pick characters / cancel), (3) whether to reset current stats to new baseline or only update the baseline for future decay/reset targets, (4) persona handling (persona isn't in the scenario — separate UI path needed). |
| B-049 | Adaptive panel — comprehensive data visibility | `new` | medium | small | The adaptive panel should surface all data that affects character behavior and session flow, not just high-level summaries. For each section that influences decisions (resistance, motivation, intensity, gates, themes, behavioral dimensions), show the full computation details: formula breakdown, input values, intermediate calculations, and resolved outputs. Each data section must be a collapsible block collapsed by default — expandable on demand for debugging and power-user insight. Includes: motivation score formula decomposition, resistance band resolution path, narrative gate metric values per character, theme score/intensity/link contributions, behavioral dimension drift details, and stat decay/reset targets. |
| B-054 | BehavioralFrameInjector — real behavioral/stat injection + resistance/willingness HARD CONSTRAINT injectors | `planned` | high | medium | Consolidate frame+stat injection into BehavioralFrameInjector at Pri 20. Remove redundant injections. Add StatProfileDirectiveInjector at Pri 65. Plan: /memories/session/plan.md. |
| B-060 | HusbandAftermathInjector — per-character memory & dynamic relation labels | `designed` | high | small | ShouldFire gates on whether the current actor has an EncounterCompletion record (not any character). BuildText uses the actor's own record and resolves "return to your [relation]" dynamically from CharacterRoleCatalog labels instead of hardcoding "husband". Plan: specs/Planning/B-060-aftermath-injector-character-awareness.md |
| B-061 | Narrative theme gate — Min/Max interactions since commitment | `new` | medium | small | Change narrative theme gate values to use both Min and Max interactions-since-commitment. Currently only Min is evaluated; Max and other stat-value gate variants are ignored. With only Min, no behavioral change occurs on the upper bound. Adding Max enforcement means gates with a Max threshold actually block/allow passage as intended — if the value exceeds Max, the gate fails (as opposed to silently passing). Requires: (1) extending gate evaluation to support Max (upper-bound) comparison alongside existing Min (lower-bound); (2) ensuring when both Min and Max are specified, the gate passes only if the metric is within [Min, Max]; (3) ensuring Max-only or stat-value-only gates are not silently skipped. |
| B-062 | Weather & Environmental System for RP engine | `designed` | low | large | Full weather system with: (1) `WeatherCondition`/`WeatherIntensity`/`WeatherState` domain model, (2) LLM-based weather detection background job, (3) theme marker integration (`[Weather:storm:heavy]` in phase guidance), (4) `WeatherInjector` in prompt pipeline, (5) weather transition engine for dynamic shifts, (6) narrative gate modulation via `WeatherSeverity` metric, (7) intensity floor/ceiling modulation by weather, (8) pacing nudge by weather, (9) theme affinity score bonuses for weather-matched themes, (10) scenario location `TypicalWeather` seeding, (11) session-scoped weather widget UI with manual override. Design: `specs/Planning/B-062-weather-environmental-system.md`. |
| B-063 | RP session create — add/remove characters with POV validation | `new` | medium | medium | On the roleplay session creation page, add the ability to add and remove characters from the session. Must keep the POV Character valid — if the current POV character is removed, the user must select a new POV character from the remaining characters in the session. Covers: (1) add-character UI in session create flow, (2) remove-character with POV-validity enforcement, (3) POV selector update when current POV is removed. |
| B-064 | Investigate character plans/promises tracking to ensure follow-through | `new` | medium | medium | Characters make plans (e.g. "let's meet later tonight") that are then forgotten across turns and never materialize. Investigate how to track character-made plans/promises and inject them as reminders into continuation prompts so planned events actually happen. Covers: (1) research approach — plan detection method (semantic inference vs keyword), storage model, prompt injection strategy; (2) feasibility study of tracking unresolved plans across interactions; (3) design for plan extraction, persistence, and follow-through enforcement. |
| B-066 | Adaptive panel — character selection data and turn decisions | `new` | medium | small | Add a new section to the adaptive panel that surfaces character selection data and turn-by-turn decisions. Should show: which actor was selected for each turn, the scoring/ranking that led to that decision (recency, location, role, stats, phase, etc.), any override logic applied, and why other actors were not selected. Intended for debugging turn composition and overflow-continue actor selection. |
| B-067 | Multi-otherman support — role guidance and story impact | `new` | medium | large | Add support for multiple otherman characters in a session, with per-character role guidance to drive better theme direction. Covers: (1) defining each otherman's intended role in the story (e.g. primary lover, rival, casual fling, group participant, etc.), (2) how multiple othermen interact and affect the overall narrative trajectory, (3) theme-direction guidance so each otherman pushes the story toward specific themes or outcomes rather than behaving generically, (4) prompt injection to differentiate otherman personalities and motivations, (5) session data model updates to track multiple otherman assignments with role metadata. |
| B-068 | Pacing prompt injection — focused fix for slow/medium/fast pacing | `designed` | high | small | Plan: `specs/Planning/B-068-pacing-fix.md`. Phase 1 implemented: pacing-aware direction added to `TurnContextSlot` (Slot 3), pacing text removed from `UserDirectionSlot` (Slot 16), engine prompt text narrowed (beat-advancement removed, location/time hints retained). `DeepeningPolicy` now wired into position 2+ guidance. Build: 0 errors, 0 warnings. Tests: 25/25 pacing-related pass, 95/104 prompt tests pass (9 pre-existing failures unrelated). |
| B-070 | Scene transition — detect natural scene endings and advance time | `new` | high | medium | After a scene naturally concludes (character walks away, blowjob ends, goodbyes), the story continues minute-by-minute instead of jumping to a new time/location. Position 1 actor gets "advance time" but stays in place. Options: (A) Narrative-based scene-end detection — scan Narrative for conclusion signals, tag next turn with time-skip instruction; (B) keyword-based trigger on character output; (C) per-turn pacing escalation after N turns in same location; (D) stronger User Direction text making time advancement a HARD CONSTRAINT. Analysis: session 5ba08ad1, interaction db49ffbd. Options persisted in /memories/repo/scene-transition-options.md. Recommendation: start with D, add A if needed. |
| B-073 | Adaptive panel — character points, LLM prompt & include/exclude decision visibility | `designed` | medium | medium | Add a "Turn Actor Selection" section to the adaptive panel showing per-character participation points (BaseScore + per-component breakdown), the full narrative-director LLM prompt (system + user), and the decision (raw output, parsed characters, reasoning, source badge, filtered-out set). Requires: persist full prompt + raw output + score breakdown + filter reasons in new `ActorSelectionPromptSent`/`ActorSelectionResponseReceived` debug events; expose `QuerySessionEventsAsync` on `IRolePlayDebugEventSink`; render in `RolePlayWorkspace.razor`. Plan: `specs/Planning/B-073-adaptive-panel-actor-selection-prompt.md`. Open decisions: keep vs remove `OverflowActorSelection`; `ActorSelectionSource.Cache` is currently unreachable (dead code). |
| B-075 | Per-character steering with directional options | `designed` | high | medium | Expand the steering system from a single global action to per-character steering with four directional options relative to the active theme's push: (1) steer away — e.g. Wife rejects other man's advances, choice lowers Desire, raises Loyalty, etc.; (2) neutral — keep things as they are; (3) steer towards — align with where the theme is going; (4) steer hard — jump right in / push extreme. Covers: (1) per-character steering UI (target character selector), (2) four-option directional model (away / neutral / towards / hard), (3) stat delta mapping per direction (Desire, Restraint, Tension, Connection, Loyalty, etc.), (4) prompt injection describing the steering intent for the target character, (5) interaction with theme direction and phase escalation logic, (6) existing B-020 (steer commands — apply stat changes on execution). Plan: `specs/Planning/B-075-per-character-steering.md`. **Scope revised after clarification**: this feature does NOT mutate character stats directly. The four direction options are context-aware — option text is shaped by the target character's current stats (`CharacterStatTextCatalog` band text), behavioral frame, runtime encounter dimensions (Wife/Husband), active theme phase guidance, scene location, and recent interactions. Once the user picks a direction and presses Continue, the per-character directive is injected into the next continuation's prompt and the existing semantic pipeline updates stats naturally from the generated narrative (no new stat-mutation path). B-020 (direct stat mutation on steer) stays `new`/unimplemented and is out of scope. Surfaces the built prompt + LLM response in the UI (per-interaction `PromptText`/`RawResponseText` + popup preview). |
| B-083 | Character location — support locations outside the scenario list | `new` | medium | small | A character's location does not have to be one of the scenario's defined locations. When a character leaves the scenario and ends up somewhere not in the scenario location list, that out-of-scenario location must still be shown (and tracked). Covers: (1) allow free-form / unknown location values for characters, (2) display the actual location even when it is not in the scenario location list, (3) ensure location presence/actor-selection logic (IsActorInScene vs CurrentSceneLocation) handles non-scenario locations without dropping the character, (4) persistence of the out-of-scenario location. Related: B-080 (wrong scene location caused OtherMan solo turns), B-002 (locations broken/incorrect). |
| B-084 | Wire theme-driven Granularity into the prompt (currently only the manual override works) | `new` | medium | small | `SceneDirectionResolver` parses `[Granularity:micro\|meso\|macro\|montage]` into `SceneDirection.Granularity`, but no regular prompt slot renders it — only the B-082 `ContinuationOverrideSlot` emits it when the user explicitly overrides. Verified in the B-082 test matrix: the manual override produces the correct density gradient (Micro = one dense moment → Montage = "three days passed"), but a theme/scenario declaring `[Granularity:*]` has no prompt effect (see `specs/B-082-continuation-settings/research/marker-implementation-audit.md` — "never wired"). Fix: render the resolved `Granularity` (override OR theme/phase default) in a regular slot (e.g. the scene-direction / intensity slot). Same gap also affects Beat Style (`BeatScope`), Time Shift (`TimeShift`), and Scene Presence (`RequireScenePresence`) — considered here for Granularity first. |
| B-085 | Wire Beat Style (Single/Short/Episodic) override into the Climax Beat Cursor so scene length is controllable | `new` | high | medium | The encounter/scene length is meant to be controlled by the Climax Beat Cursor (32-beat sheet `1a→8g`, each beat with `MinTurnsBeforeAdvance`), but `RolePlayAssistantPrompts.IsEpisodicBeatStyle` reads ONLY the theme's `[BeatStyle:episodic]` Climax marker — the Continuation Settings "Beat Style" override (`ContinuationOverride.BeatScope`) is never consulted. Result (verified in B-082 test matrix, session 318d77a2): changing Beat Style Single/Short/Episodic in the UI only injects a soft text line and has NO behavioral effect (`0` ClimaxBeatCursor events; encounter spans varied 2–10 turns uncontrolled). Fix: resolve `BeatScope` (override ?? theme marker) and feed it into (a) cursor activation and (b) per-beat turn budgeting — Single ≈ reach climax in ~1 turn, Short ≈ 2–3 turns, Episodic = full beat sheet. Also scope the directive text to the encounter arc (kiss→…→climax), not the ambiguous "this moment". Related: B-084 (same override-vs-theme wiring class), B-006 (beat sheet origin), B-052 (prompt-injection contradiction cleanup). |
| B-086 | Character introduction — describe physical attributes on first appearance; Wife notices OtherMan | `new` | medium | medium | When a character is first introduced into the session, their physical attributes should be described in the narrative. In particular, when an OtherMan character is first introduced, the Wife should notice his physical characteristics. Covers: (1) a first-appearance trigger that injects a physical-description directive for the newly introduced character, (2) physical attributes rendered from the character's data (see B-025 physical attributes / B-079 attractiveness tier catalog) at introduction, (3) role-aware framing — Wife notices the OtherMan's physique/appearance when he first appears, (4) integration with the introduction/encounter-start flow. Related: B-025 (standard physical attributes), B-079 (attractiveness tier catalog prose), B-059 (encounter-start detection), B-078 (OtherMan seduction archetype). |
| B-089 | Continuation settings redesign — collapse to Tempo + Span controls | `implemented` | high | large | Full continuation/pacing audit (2026-08-18) found the 9-setting popup over-complicated and self-contradictory: Pacing is dead for position 2+/Narrative; Fast only fires on a multi-turn beat's final turn; Climax defaults to TimeShift=Medium contradicting "stay in moment"; Granularity force-overridden in multi-turn beats; Scene Presence redundant with TimeShift=None; `quick-finish` marker dead; SteeringProfile word range inert (marker range wins). Redesign: replace the 9 dials with TWO coordinated controls — **Tempo** (Linger/Steady/Push/Leap → coherent bundle of Pacing+TimeShift+Granularity) and **Span** (Moment/Scene/Day/Montage → BeatScope budget). Engine time-skip machine consumes the same resolved Tempo (fixes two-layer time-control conflict). Move mutual-exclusion checks into the resolver (fail-fast, not UI-only). Delete dead surface (quick-finish, Scene Presence). **Implemented 2026-08-19 — all T1–T8 landed, full suite 1067/1067 green.** Design + plan + implementation record: `specs/Planning/B-089-tempo-span-design.md`. Related: B-082 (popup), B-084 (granularity wiring), B-085 (beat style override), B-052 (contradiction cleanup). Remaining follow-up: Theme Guidance UI in the theme editor (marker-authoring surface). |
| B-112 | Local uncensored FLUX.1-dev on the RTX 5080 host (ComfyUI) + app image-model integration | `implemented` | medium | medium | BigLust/SDXL (CLIP) cannot bind blocking/arrangement from text; need text-driven composition for implied/softcore NON-explicit scenes (no nudity). LM Studio cannot run diffusion (LLM-only), so the target host runs ComfyUI + uncensored/abliterated FLUX.1-dev (fp8 fits 16 GB; cu128; FluxGuidance 3.5 + cfg 1.0; empty negative). Plan: `specs/Planning/B-112-flux-local-5080-host/plan.md`. Runbook (what the host must install/configure): `docs/flux-local-5080-comfyui-setup.md`. Proof assets: `helpers/flux-local-host/`. Resolved 2026-09-07: Route 1 = stock fp8 (optional unlock LoRA; non-explicit scenes). App integration LANDED 2026-09-08: new Flux scene-image family (Flux/FluxNaturalLanguage) + ComfyUI `BuildFluxWorkflow` + `FluxSceneImagePromptCompiler` (shared NL brief, empty negative) + validation arms + dropdowns; local FLUX row ENABLED on dev DB (additive — NOT the RolePlaySceneImage default); live campfire-couple render PASS on the 5080 host. Follow-ups: Flux-grounded LLM system prompt (currently SDXL-branded); stock-FLUX failed 2/4 implied-proof cells on composition fidelity (kneeling-implied, garden-fours) — unlock LoRA / prompt tuning. Related: B-111 (visual production program), B-100. |
| B-113 | Scene video production — Wan 2.2 uncensored I2V/FLF2V over rendered keyframes + local ComfyUI video setup | `designed` | low | large | Video = animate between already-rendered identity-consistent keyframes (NOT text-to-video), plugged into the B-100/B-111 beat→keyframe seam. Research 2026-09-08 verdict: commercial APIs (Veo/Sora/Runway/Kling) block adult content → out; **Wan 2.2** (Apache-2.0, ComfyUI-native) is the confirmed path with a real uncensored ecosystem (`rzgar/wan2.2-i2v-A14B-Uncensored-base` ~7/10 anatomy, `rzgar/Wan2.2_LightX2V_4Step_Uncensored` I2V+T2V 4-step pair + CubeyAI LoRAs 0.5–0.55); **MiniMax H3** (33B, FL2VA/Ref2VA = first/last-frame + character-reference, native audio) is technically best but **US/EU/UK/KR are Excluded Territories** in its community license (formal MiniMax application required) → watch gate B-114; **Wan 3.0** repo ships no weights (README only, verified 2026-09-08) → watch gate B-115. Two execution tiers: local 5080 = 5B class (TI2V-5B / GGUF 14B), RunPod serverless = 14B uncensored I2V. Local ComfyUI video setup on WOOD-GAME-MAIN (ComfyUI 0.34.0 @ http://192.168.0.16:8188) is proof-first, same discipline as B-112. Full research + decisions + setup steps: `specs/Planning/B-113-scene-video-production/plan.md`. Related: B-100, B-111, B-032, B-112. |
| B-114 | Video evaluation — MiniMax H3 (territory/license gate) | `new` | low | small | MiniMax H3 (33B open weights, trending ~5M dl/mo, ComfyUI templates exist) is technically the strongest video candidate — **FL2VA** first/last-frame + **Ref2VA** omni-reference (≤9 images/3 clips/3 audio = character-reference identity) + native stereo audio. Blocked for this project: the MiniMax H3 Community License (2026-08-02) makes **EU/UK/South Korea/US Excluded Territories** (this dev host is US-based) requiring a formal MiniMax application (`platform.minimax.io/h3-license`); hosted Context-IR/2K path is moderated and blocks adult content; AUP itself does not explicitly ban adult content; §V forbids using Outputs to improve other AI models. Revisit if a license is granted or territory scope changes. Watch details: `specs/Planning/B-113-scene-video-production/plan.md` §1.3. |
| B-115 | Video watch — Wan 3.0 open-weight release | `new` | low | small | Wan 3.0 announced (~2026-09-04): realistic human rendering, consistency across characters/props/space/style, native audio, 2–30 s, first/last-frame control, editing + extension — a near-perfect fit for DreamGen's identity+coverage needs. HF repo `wanvideo/wan-3-0-video` ships **no weights** (README + .gitattributes only, verified 2026-09-08) and is webapp-only (`wan30.io`). Re-check when the model card gains file entries or ComfyUI templates; do not build until open weights land. Watch details: `specs/Planning/B-113-scene-video-production/plan.md` §1.4. |
| B-116 | Local img2img "re-skin" re-render from a source scene image (local ComfyUI) | `designed` | medium | medium | Add a local (free) img2img re-render operation: take an existing rendered scene image as the source (LoadImage → VAEEncode → KSampler with denoise < 1) and re-render it onto a target scene/prompt with a local ComfyUI generation model, preserving layout/pose while transplanting the environment. Verified 2026-09-08 (local FLUX.1-dev fp8 on WOOD-GAME-MAIN): a dictated two-person pose is fragile in text-T2I, but img2img over the pose base **holds the geometry at denoise 0.45–0.75 and transplants the environment (studio/grass → wooded/sun) at ~0.75** (outputs in git-ignored `artifacts/tmp/images/flux-pose-reskin/`; hand-run workflow `artifacts/tmp/dbquery/workflows/flux-reskin-{045,060,075}.json`). Today every `ComfyUIImageClient` render is T2I-only (`denoise = 1.0` + `EmptyLatentImage`); all source-image ops route to the cloud Qwen editor, so local FLUX/SDXL cannot re-render an existing scene image. Scope: new `SceneImageOperation.Reskin` + `SceneReskinRequest {SourceImageId, Denoise, Prompt, ModelId}` + ComfyUI i2i workflow builders (FLUX first — proven; BigLust/Juggernaut flagged capable but experimental) + `scene-image-reskin` job + `LocalImg2Img` capability token in `SupportedVisualStrategiesJson` (idempotent dbq `local-img2img-configure`) + Studio/editor UI (denoise slider, default 0.75, fail-fast no-fallback gating). Additive; never repoints the default; does not touch Generate/Edit/Compose/identity paths. Plan: `specs/Planning/B-116-local-img2img-reskin/plan.md`. Related: B-112 (FLUX local host + app integration this extends), B-100, B-111, B-032. |
| B-117 | Pose-controlled scene compositions via local ComfyUI ControlNet (DWPose + OpenPoseXL2) | `designed` | medium | large | **Program:** `specs/Planning/identity-lora-program-map.md` — stage 4; consumes B-118's pose store, consumed by B-123 (pose-conditioned cells). Plan: `specs/Planning/B-117-pose-controlnet-render/plan.md`. Make a scene-image Composition actually follow an authored blocking/pose, not just text. Gap (verified 2026-09-08): scene images are T2I-only — the Composition Composer sends a text prompt; no pose image / skeleton / ControlNet conditioning is ever attached to a render. The app's only "ControlNet" surface is the Location reference strategy (resolver classifies it a graph strategy, UI offers it for Location, Model Manager has a "ControlNet location control" flag) but **no render code executes it** and no model declares it (`SupportedVisualStrategiesJson=[]` on all 5 local rows). DWPose has zero in-app surface. Local ComfyUI host (WOOD-GAME-MAIN 5080 @ 192.168.0.16) is now proof-verified 2026-09-08/09 for the full stack: `comfyui_controlnet_aux` DWPreprocessor + `D:\ComfyUI\models\controlnet\thibaud-openpose-xl2\OpenPoseXL2.safetensors` (5,004,167,829 B) + seeded DWPose TS ckpts; SDXL/BigLust proof PASS (extract + ControlNet render); Pony-family proof pending before enabling Pony. Scope (mirrors the identity path): attach a pose (image/skeleton from DWPose-extract or the OpenPose library) to a composition render; new per-model ControlNet capability declaration (weight ref + strength default, fail-fast) in Model Manager; extend `ComfyUIImageClient` with an OpenPose ControlNet workflow builder (LoadImage pose → DWPreprocessor → ControlNetLoader/Apply → KSampler, strength 0.35–0.85 per compiler-standards); route in `SceneImageRenderingJobHandler` only when the resolved model declares+qualifies ControlNet on a ComfyUI provider (no fallback); pose picker + strength UI in the Composition Composer; record snapshot on `SceneImageRecord`. Follows `.github/instructions/scene-image-prompt-compiler-standards.instructions.md` (canonical OpenPose hard rules); node-graph template `helpers/runpod/workflows/juggernaut-fellatio-openpose.json`. Related: B-032 (pose-editor plan is the bigger in-app editor), B-116 (adjacent local-ComfyUI op), B-112. |
| B-118 | Pose Studio — 2D wireframe pose editor + DWPose extract-from-image + reusable pose library feeding composition renders | `designed` | medium | large | **Program:** `specs/Planning/identity-lora-program-map.md` — stage 3; **owns the pose store + DWPose extract + pose library** that B-120 (derived records) and B-123 (picker) consume. Plan: `specs/Planning/B-118-pose-studio/plan.md` (search / extract / edit / **save new** / **apply** — one render at a time, never a sweep). Build the in-app pose tool the B-032 phase-3 pose-editor plan targets, as a pragmatic 2D slice (NOT the full Three.js 3D rig editor). Workflow: pick an existing scene image that is "almost right" → DWPose-extract its skeleton + COCO body keypoints on the LOCAL ComfyUI (DWPreprocessor already proof-verified 2026-09-08/09) → load into a 2D canvas editor → drag joints for a minor tweak / position change → save as a reusable named pose → use it as the Pose reference on the next Composition render via the B-117 OpenPoseXL2 ControlNet path (same prompt, new pose = pose re-shoot). Seed the library from the existing 472-pose NSFW OpenPose pack (`helpers/runpod/openposeNSWFPosePackage_final/`, git-tracked: category/<res>/<name>.json + pre-rendered bone_structure/depth PNGs; single-person only; only standing/squatting/kneeling-feet-on-ground verified to HOLD under OpenPoseXL2 — tag known-good). Design defaults: edit COCO-18 body joints only (hands/face not reliably honored by OpenPoseXL2); dedicated lightweight `PosePreset` store (name, category, keypoint JSON, skeleton PNG, thumbnail, known-good flag) rather than shoehorning SceneAsset; editor = Blazor + JS canvas drag. Slices: (1) PosePreset store + pack seed importer; (2) DWPose extract-from-image client + live JSON-capture proof; (3) 2D editor (render/edit/save/mirror/undo); (4) Composer pose picker = saved presets (feeds B-117). Depends on B-117 (render path). Related: B-032 pose-editor-plan.md (3D reference), B-117. |
| B-119 | Multi-character layout toolbox + scene-image production decision workflow (depth/canny ControlNet from a reference + regional identity + FLUX.2 verdict) | `designed` | medium | medium | The user-facing "what tool for which situation" layer over the scene-image stack, complementing (NOT duplicating) canonical B-117 (OpenPose pose-control composition render) and B-118 (Pose Studio). Contains the decision ladder for any beat image request: **Route A** single-subject/standard close 2-shot = FLUX/BigLust T2I (exists today); **Route B** layout already exists as an image = img2img re-skin (B-116); **Route C** dictated/awkward multi-char blocking branches C1 = **Depth/Canny ControlNet from a reference photo** (best for reclining/multi-body) + regional IP-Adapter identity (B-119 scope) / C2 = OpenPose skeleton (canonical B-117; upright only — lying fails) / C3 = FLUX blocking + mild anchor then re-skin; then location via plate, identity LAST, explicit LAST. Feasibility verified against local ComfyUI 0.34.0 @ 192.168.0.16 (2026-09-09): host already has OpenPoseXL2 weight + full controlnet_aux preprocessors (depth/canny/seg) + Impact Pack (regional) + IPAdapterRegionalConditioning; missing only depth/canny ControlNet weights (install in T01). **FLUX.2 (incl. Kontext) does NOT run on the 5080 16 GB** (12B family, VRAM wall) → cloud-only via TogetherAI `black-forest-labs/FLUX.2-pro` (already registered). Files: `specs/Planning/B-119-multi-character-layout-workflow/{plan,workflow,tasks}.md` — workflow.md is the NOT-strict ladder. Additive; capability-gated, no fallbacks; never repoints defaults. Related: B-117, B-118, B-116, B-032, B-112, B-111, B-100. |
| B-120 | Asset Manager: structure/layout extraction from source images (derived Depth/Canny/OpenPose/Seg control assets) | `designed` | medium | medium | The Asset-Manager slice that CREATES layout/control images FROM source images — the production half that the B-117 (OpenPose pose-control) and B-119 (depth/canny ControlNet) renders consume. Seams already exist: `SceneAssetType.Location`, reference-bootstrap LocationProfile/LocationReference, `ProducedImageModels` Location/Wardrobe reference kinds, and `ReferenceStrategyResolver` already classifies `ControlNet` as a graph strategy (hosted APIs can't run it). Missing = the derived-asset pipeline: (1) user imports/picks a source image (approved render, real photo, location plate) into the asset catalog and declares intent (Location / Pose / Layout); (2) a local-ComfyUI extraction job runs a preprocessor (verified on host: DepthAnything/MiDaS/Zoe/Metric3D depth, Canny/PyraCanny, multi-person OpenPose/DWPose, OneFormer/UniFormer segmentation; raw image kept as the location plate for B-116 re-skin); (3) stores a versioned/approved DERIVED asset (extractor type, source provenance + sha, params, thumbnail) reusing identity/wardrobe approval + delete-in-use guards; (4) semantic bindings ("canonical blocking for pine-clearing rock"); (5) composition render consumes it as `LayoutReferenceImageId + StructureType + strength`, resolver runs a ControlNet render only when the model declares the graph strategy (no fallback). Coordinate boundaries: poses share the B-118 pose-library store; the 2D pose-editor UX stays B-118; depth/canny render wiring stays B-119; OpenPose render stays B-117 — B-120 is the shared derived-asset store + extraction jobs + bindings. Files: `specs/Planning/B-120-asset-layout-extraction/{plan,tasks}.md`. Related: B-117, B-118, B-119, B-116, B-108, B-032. |
| B-121 | Character Identity Studio — in-app end-to-end reference-pack pipeline (front → validate → de-clothe → crop → enhance → angles → pack) | `planned` | high | large | **Program:** `specs/Planning/identity-lora-program-map.md` — **stage 1 and the producer of shared machinery** for the identity→body→LoRA spine. Delivers to B-122/B-123: the resumable step pipeline, the editable prompt-template store, the eye/face-landmark validation capability with manual override, reference quality gating, the same-image identity-edit primitive, and view-tagged promotion. Must stay **target-extensible** (FR21-035) so B-122's body pack is a new target kind, not a second studio. **Plan only — handoff to an implementing agent; no code written under this item.** Plan set: `specs/Planning/B-121-character-identity-studio/{README,spec,plan,tasks,ui-contract,seed-prompts}.md`. Turn the hand-run Dean v8 / Becky v5 pack build (2026-09-10) into a guided, resumable, in-app workflow so the user can build the next character (Sam Winchester) entirely in the app. Steps: (1) front — generate candidates OR upload an existing image; (2) validate eyes/proportions; (3) remove clothes (neck-preserving); (4) crop/framing normalise; (5) enhance 4× upscale → clean 1024; (6) 3/4 left+right, then profiles; (7) promote into a new draft pack + sync test refs. Reuses `IImageEditingClient.EditAsync`, `IImageGenerationClient`, `SceneAssetService.CreateFromUploadAsync`, `ICharacterImageIdentityService` (draft/supersede/approve), durable jobs, and `ReferenceImageQualityAnalyzer`. **Two genuine gaps:** (a) the app has NO face-landmark capability — the only trustworthy eye check is `tools/eye-validation/measure_iris.py` (MediaPipe), so the step needs a subprocess call to it, a .NET port, or a manual visual gate; (b) the app has NO upscaler — only a git-ignored dev binary in `artifacts/tmp/realesrgan/` and a *test runner* workflow (`identity-two-character/runners/run_upscale.py`, 4x-UltraSharp via ComfyUI). Also missing: crop, garment-removal, and any UI caller for `SceneAssetProfilePackJobHandler` (its `AngleEdits` are DI-registered but unreachable since commit `930374f`, and its prompts contain the clothing-invention + direction bugs fixed by hand in this session). Must bake in the hard-won constants: local Qwen Image Edit 2511 at 40 steps/CFG 4/euler/simple/shift 3.1 (NOT the Rapid-AIO 8-step variant), neck-protection clause in the garment prompt, no "clothing" in angle preserve-lists, image-space direction wording + **yaw-sign gate** with mirroring fallback, the view convention (Profile/ThreeQuarter **Left faces image-left**), the analyzer's ≥250 sharpness bar, and the four ref locations that must be synced on promote. **Every prompt is a user-editable persisted template** — the prompts validated in the hand-run session ship as *seeds* (with an immutable `SeedBody` so Reset-to-default restores them exactly), resolvable at character-override → global, failing fast by key when absent; no prompt string is embedded in code to fall back to. **Sequencing = B-108 first, but B-108's `tasks.md` is OBSOLETE:** B-108 was absorbed into B-111 Phase 2 and most of it is already implemented (`ReferenceBootstrapBatch`, `ReferenceBootstrapRepository`, `ReferenceBootstrapService` with batch creation + durable candidate generation + decisions + four promote paths, `ReferenceBootstrapPanel.razor` mounted in `AssetStudio.razor`, `CandidateGrid`/Review Deck, tests). Do NOT re-implement B108-001…010 or B108-015…023. What genuinely does not exist: a prompt-template store, any .NET face-landmark capability, a crop step, an in-app upscaler, a build/step pipeline record, and any view tag on face promotion (`PromoteAcceptedCharacterFaceAsync` hardcodes `Front`). Related: B-108, B-111 (P2 absorbed B-108), B-107, B-106, B-120, B-032. |

| B-122 | Body-complete character identity pack — extend the identity pack from five face views to the full body (BodyCard + full-body multi-angle references + wardrobe/body split + body-invariant validation) | `planned` | high | medium | **Program:** `specs/Planning/identity-lora-program-map.md` — stage 2 of the identity→body→LoRA spine; hard-blocks B-123. Phase 0 must be a **new target kind in B-121's pipeline** (B-121 FR21-035), not a second studio, and B-121 is a hard prerequisite (it delivers the pipeline, template store, eye-check capability and view-tagged promotion). Prerequisite for character LoRA training data. Today the approved pack is face-only (front + 3/4L/R + profile L/R); the capture list (`specs/image-generator-tests/flux-character-lora/DATASET-CAPTURE-LIST.md`) already defines what "body-complete" means — a canonical BodyCard (shape/build/skin/body-hair/tattoos/scars/grooming, resolving all `[DECIDE]` items) plus full-body references (clothed + unclothed) that make body shape and marks consistent across the set. Reuses `ICharacterImageIdentityService` (draft/supersede/approve), Qwen full-body edit to stamp marks/shape, and the eye-validation discipline extended to body invariants. Plan: `specs/Planning/B-122-body-complete-identity-and-lora-image-studio/plan.md` Phase 0. Related: B-121 (identity studio this extends), B-123 (the consumer). | 
| B-124 | Reference model foundation + Asset Manager grouped shell — the first thing built | `planned` | high | medium | **Stage 1 of the identity→body→LoRA workstream.** Owns the reference data model (additive `ViewDescriptorJson` on both asset tables, `CanonicalFullBodyAssetId`, the canonical-slot + descriptor split, and the required-set approval rule) **and** the grouped Asset Manager shell (Character → Identity pack → view set → assets; **no flat mode**), surfacing the identity store alongside the library. Built before any feature stage so B-121/B-122/B-123 consume a stable schema instead of each defining a slice and drifting. Plan: `specs/Planning/B-124-reference-model-and-asset-manager-shell/plan.md`. See `identity-and-reference-model.md` §2/§4. |
| B-123 | Character LoRA Image Studio — fully functional in-app workflow that generates, normalizes, quality-gates, captions, registers, freezes and exports a character's LoRA training image set (~30 core + ~6 variation cells, clothed + nude) | `planned` | high | large | **Program:** `specs/Planning/identity-lora-program-map.md` — stages 3–4 of the identity→body→LoRA spine; depends on B-121 (machinery) and B-122 Phase 0 (BodyCard, hard-blocking). **Ownership corrected:** it **depends on and sequences** B-117/B-118/B-120 — it does not build them (B-119/B-120 claim those areas); the only prerequisite it *owns* is LoRA inference wiring. Replaces the raw-JSON "wireframe" (`LoraDatasetGeneration.razor`) with a guided, automated flow. Closes five verified gaps: (1) no member registration (`AddDatasetMemberAsync` only called from tests), (2) no coverage-plan generator (user hand-writes JSON), (3) no caption builder (typed manually), (4) no pose/DWPose surface, (5) no automated quality gates. Phases: coverage-plan generator over the FR2-047 axes (angle/crop/expression/framing/pose/wardrobe/lighting/background/aspect) with fixed per-cell seeds; per-cell generation reusing `CharacterAssetGenerationService` + `SceneImageService`, pose-conditioned cells via B-117 ControlNet + the 472-pose library + head-keypoint validation, lying/all-fours cells via B-119 depth route (OpenPoseXL2 re-poses them); Qwen face-only + full-body normalization; automated gates (face count, identity vs canonical, eye check, near-duplicate) plus a NEW joint-geometry pose-adherence scorer (raster-IoU proved invalid); caption template `{trigger}, {pose/angle}, {distance}, {clothing}, {lighting}, {background}, {expression}` with invariant identity never captioned; freeze + `.txt`/`dataset.toml` export into the existing training dispatch. Plan: `specs/Planning/B-122-body-complete-identity-and-lora-image-studio/plan.md` Phases 1-8 (NSFW-inclusive: clothed + nude cells). **Ownership corrected (see `specs/Planning/identity-lora-program-map.md` §3):** it **depends on and sequences** the B-117 ControlNet render, the B-118 pose store/DW extract and the B-120 derived assets — it does **not** build them (B-119/B-120 explicitly claim those areas, and building them here would duplicate). The only prerequisite it **owns** is the LoRA inference path: `LoraLoader` graph node + trigger-token injection + per-base-model artifact selection (`LoraLoader`/trigger-injection path). Non-goals: two-person LoRAs and training-service changes. | 
| # | Title | State | Priority | Scope | Notes |
|---|---|---|---|---|---|
| B-003 | Test multiple themes active simultaneously | `done done` | - | - | Not implemented; no longer required |
| B-006 | Ensure sex scenes are more detailed and explicit | `done` | - | - | Prompt framing and continuation guidance for climax-phase scenes needs to direct more explicit and descriptive output. Plan: `specs/006-explicit-scene-writing/plan.md`. Implemented: static prompt improvements (US1/US2) + configurable SceneDirective per intensity profile (US3). 37 unit tests passing. Phase 9 complete: DB-backed beat sub-beat cursor (32 canonical beats, 1a→8g), beat advance logic in engine, beat context prompt injection in continuation service, Beat Sheet CRUD tab in ThemeProfiles.razor. Build: 0 errors. |
| B-007 | BUG: Characters mixing dialogue — wrong actor speaks wrong lines | `done done` | - | - | Not implemented; no longer required |
| B-008 | Change RP session start — allow single theme or theme profile override | `implemented` | - | - | On session create, user can pick a single RPTheme or a Theme Profile that overrides the scenario's default for that session |
| B-009 | Wife resistance & cheating motivation gap | `done done` | high | medium | The Wife character says "we shouldn't" but never genuinely resists advances because escalation pressure has no counterweight. Also missing: motivational drivers for why a wife would cheat (emotional neglect, sexual frustration, etc.). Analysis: `specs/Planning/WifeResistance.md`. **Superseded by the unified Wife Willingness to Cheat algorithm — see `specs/002-wife-willingness-to-cheat/plan.md` (B-034)** **[Audit 2026-08-19]** Superseded by the unified Wife Willingness to Cheat algorithm (B-034, implemented). |
| B-009 | UI cleanup — dashboard with recent activity | `done done` | - | - | Nav redesigned: 4 collapsible groups (Play/Content/Configuration/System), sidebar full-collapse toggle (localStorage), About link removed, top-row eliminated. Counter+Weather deleted. Templates moved into Profiles tab (TemplatesPanel.razor). Home.razor → dashboard (recent RP/story sessions, queue status, quick actions). Build: 0 errors (web project). |
| B-010 | Keep scroll position when new responses are added | `implemented` | - | - | Read cursor divider (— new —) rendered between interactions at a tracked index. On Continue: if NOT at bottom, cursor placed at pre-generate boundary and scroll jumps to it; if at bottom, existing follow behavior retained. IntersectionObserver advances cursor as interactions scroll past viewport. Session load starts fully-read (cursor hidden). Build: 0 errors. |
| B-012 | HP2 Issue 3: Diagnostics readability — remaining tests and HP2 manual pass | `done done` | - | - | Not implemented; no longer required |
| B-013 | HP2 Issue 5: Writing style profile model simplification decision | `done done` | - | - | Not implemented; no longer required |
| B-014 | HP2 Issue 6: Narrative prompt intensity/style composition — validation and fix | `done done` | - | - | Not implemented; no longer required |
| B-015 | HP2 Issue 8: Stat keyword categories — complete CRUD UI and design pass | `done done` | - | - | Not implemented; no longer required |
| B-021 | Character stats UI — show baseline and phase-unlock requirements | `implemented` | - | - | Adaptive panel now shows 5 columns: Stat / Base / Now / Fit / Gate. Base = per-character baseline sealed at session start (or updated on Apply). Fit column shows ✓ or gap vs active theme FitRules for the character's exact role (no Any fallback). Gate column shows ✓ or gap vs current-phase narrative gate rule. dLast column removed. Reset-phase decay now targets per-character BaselineStats instead of global 50 default. Apply button updates both live stats and baseline. |
| B-024 | Investigate and fix Narrative prompt issues | `implemented` | high | medium | Root cause analysis complete: 8 issues identified — (1) Atmospheric intensity override removed: narrative now uses session resolved intensity like all character interactions; (2) opening/standalone narrative bypasses validation pipeline; (3) FirstPersonLeakRegex fires on quoted dialogue; (4) character interiority not triggering retry; (5) generic correction prompt; (6) writing instruction under-specifies physical scene categories (surroundings, positions, intimate detail); (7) dialogue suppression too weak / Climax threshold too high; (8) location display title leaks into narrative output. Spec, research, data-model, and plan in specs/024-narrative-prompt-fix/. **[Audit 2026-08-19]** All 24 tasks done; code verified (narrative intensity override, validation retry, correction prompt). |
| B-025 | Template Character and Persona — standard physical attributes | `implemented` | medium | large | Add a standard set of physical descriptors to Character and Persona templates. Structured fields (not free-text only) so they can be consistently injected into prompts and displayed in the UI. Full attribute list: height, weight, hair colour/style, eye colour, body type, skin tone, ethnicity, age, bust/waist/hip measurements, endowment length, endowment girth, distinguishing marks (free-text), piercings (free-text), tattoos (free-text), clothing/style (free-text), overall attractiveness (scale 1–10). Fields should be persisted on TemplateDefinition and Character, with a dedicated formatted appearance block injected into continuation prompts. **[Audit 2026-08-19]** All 4 user stories done; `PhysicalAttributes` + `Formatter` + `Editor` in code. |
| B-026 | Prompt response renders as received — streaming display | `implemented` | - | - | Render AI response text progressively as chunks are received from the model rather than waiting for the full response to complete before displaying. Improves perceived responsiveness for long interactions |
| B-027 | Prompt queue — continue processing if workspace is navigated away | `done` | medium | large | Submitted RP continuation prompts should continue processing in the background if the user navigates away from the workspace. On return, completed responses should be present in the session. Requires background task persistence that survives page navigation without cancelling in-flight LLM requests. Spec: `specs/027-prompt-queue-continue/spec.md`. Scope: same-process navigation only (in-memory tracker, no DB persistence). Approach: new singleton `IRolePlaySubmissionTracker` keyed by session ID; engine chunk callback made nullable; component submits-and-returns instead of awaiting; silent response delivery via history on return. |
| B-028 | RP session — scenario and theme visibility, default title, and editable title | `done` | - | - | (1) Dashboard session cards and session view page should display the active scenario and assigned themes. (2) When creating a new session, default the session title to the selected scenario name. (3) Allow the session title to be edited after the session is created |
| B-029 | Finish implementing the finishing move matrix | `implemented` | high | medium | The finishing move matrix is partially implemented; complete the remaining logic, configuration, and UI so the full matrix is operational in the RP engine **[Audit 2026-08-19]** 5 finish catalogs + seed services, CRUD, band filtering done. |
| B-031 | High-level design — Roleplay to Story conversion | `implemented` | low | medium | Design how a completed or in-progress RP session can be converted into a structured story artifact; covers extraction of interactions, narrative shaping, chapter/scene mapping, and output format; design only — implementation is a separate item **[Audit 2026-08-19]** `StorySummaryService`/`StoryAnalysisService`/`StoryRankingService` + Summarize/Analyze/Rank UI. Original 'design only' note is now stale. |
| B-033 | Steer Position and Full Position list | `implemented` | - | - | Configurable list of sexual positions used to steer narrative action; covers domain model, persistence, and injection into RP continuation prompts so the engine can direct or suggest specific positions during scenes |
| B-034 | Wife Willingness — refactor and implement | `implemented` | high | large | Refactor the existing woman/wife willingness concept into a clean, fully implemented feature; covers domain model revision, persistence, prompt injection, UI CRUD, and integration with the RP engine phase/escalation logic. **Design complete: `specs/002-wife-willingness-to-cheat/plan.md` — unified "Wife Willingness to Cheat" algorithm (verdict = effective Loyalty via Resistance Profile; ceiling = Desire via Willingness Profile; both injected to prompts; NTR themes consume the verdict flag)** **[Audit 2026-08-19]** `WifeWillingnessCalculator` + verdict/ceiling prompt injection; NTR consumption (tasks.md stale/unchecked). |
| B-035 | Pre-BuildUp phase — theme selection and steering | `done` | medium | large | New narrative phase that runs before BuildUp, giving more time for a theme to emerge or be selected; includes steering options (player actions or AI-driven nudges) that guide the session toward or away from available themes, with outcomes that can adjust character stats (Desire, Restraint, Tension, Connection) and influence which theme becomes active |
| B-038 | Remove V1 adaptive state — full V2 consolidation | `done` | - | - | Eliminate `RolePlayAdaptiveState` and `Sessions.AdaptiveStateJson` entirely. Promote `AdaptiveScenarioState` (V2) to the single in-memory and persisted adaptive-state model. Phases: (1) extend V2 schema with new tables `RolePlayV2ThemeScores`, `RolePlayV2ThemeTrackerMeta`, `RolePlayV2ScenarioHistory`, `RolePlayV2PairwiseStats`, `RolePlayV2SemanticEvents` and additive columns on `RolePlayV2AdaptiveStates`; extend `CharacterStatProfileV2` with Baseline/LastStatDeltas; extend `AdaptiveScenarioState` with theme tracker + scenario history + pairwise stats + semantic telemetry — DONE; (2) purge all existing roleplay sessions at cutover (no backfill); (3) move all read sites from V1 → V2; (4) move all write sites from V1 → V2 (theme scoring, semantic events, scenario completion); (5) migrate prompt builders + UI to V2 (CharacterId-only keying); (6) delete V1 `RolePlayAdaptiveState` and 11 sub-types, delete the V1↔V2 sync bridge methods (`HydrateV2State`, `MapToV2State`, `SyncSessionAdaptiveStateFromV2`, `AlignPromptNarrativeStateWithV2Async`); (7) drop `Sessions.AdaptiveStateJson` column — PENDING next phase; (8) rewrite tests against V2 — DONE. Build: 0 errors, 0 warnings. Tests: 607 pass, 15 pre-existing behavioral failures (unrelated to V1→V2 migration — phase lifecycle service behavior, semantic telemetry, theme guidance prompt content). See `/memories/session/plan.md` for details. |
| B-039 | Semantic analysis — dedicated model manager and concurrent processing | `done` | low | large | Give the semantic analysis pipeline its own independent model manager instance (separate from the main RP/continuation model manager) so model selection, concurrency limits, and queue depth can be tuned independently. Additionally, make semantic analysis process multiple interactions concurrently rather than sequentially, with configurable parallelism degree; covers service design, DI wiring, concurrency control, and UI settings surface for the dedicated semantic model slot. |
| B-041 | Session memory context — intimate encounter history injection | `done` | medium | large | Characters currently behave as if each new arc is the first time an encounter has occurred, even when the same scenario has already been completed earlier in the same session. Root cause: (1) the LLM context window does not retain early turns from long sessions, and (2) the engine never injects structured history into the prompt. Fix: after each arc completion, generate and persist a short prose summary of the encounter (scenario, phase peak, notable beats, approximate intensity); on each subsequent prompt build, inject the N most recent summaries as a "memory context" block so the AI knows what has already happened. Design must decide: summary generation trigger (on Reset entry vs. on BuildUp entry of next arc), summary length and format, where summaries are stored (new DB table or extension of `RolePlayV2CompletionMetadata`), how many prior summaries to include, and how the injection block is positioned in the system prompt relative to other guidance. |
| B-042 | Unify character stats profiles with husband awareness profile | `done` | high | large | Merge the husband willingness/awareness profile concept into a generalised character stats profile so that all characters can have a stats profile (Desire, Restraint, Tension, Connection, etc.) with the same awareness/sensitivity behaviour currently only on the husband profile; covers domain model consolidation, persistence, prompt injection, and UI. Design complete: spec.md, plan.md, research.md, data-model.md, quickstart.md, contracts/ all in specs/development/. |
| B-043 | Character profile — stat-driven instruction text generation and profile ladder design | `implemented` | high | large | Enhance character profiles so that each stat (Desire, Restraint, Tension, Connection, etc.) has seeded instruction text ranges that are automatically selected and updated as stat values change, with the current text injected into continuation prompts. Also requires design decision: should the character profile itself be a ladder (profile archetype advances — e.g. prude → curious → willing → nymphomaniac — as stats cross thresholds) or should profiles be sticky labels with only stats and generated text changing (e.g. a prude wife can reach 100 Desire without her profile label changing)? Design must cover the ladder vs. sticky decision, threshold definitions, archetype transitions if ladder is chosen, seed data for instruction text bands, and prompt injection position. Design complete: spec.md, plan.md, research.md, data-model.md, quickstart.md in specs/001-stat-char-text-drift/. Decisions: sticky profiles, text-first architecture (4 bands per stat per role), encounter dimension drift via StatToDimensionMappings, stat reduction to 5 (Tension/Connection removed). **[Audit 2026-08-19]** `CharacterStatTextCatalog`, `StatToDimensionMappings`, 5-stat reduction done. |
| B-044 | Replace interactions with turns throughout RP engine and data model | `implemented` | high | medium | Change the phase-transition threshold unit from interactions to turns (1 turn = ~3 interactions). Covers: (1) rename all underlying data fields and column names from `*Interaction*` to `*Turn*` on theme/scenario phase guidance entities; (2) update RP engine phase-advance logic to count and compare turns instead of interactions; (3) data migration — convert existing numeric values by dividing by 3 and rounding up; (4) update all prompt injection references; (5) fully update the RP workspace adaptive panel to display and label values as turns. Spec: specs/001-replace-interactions-turns/spec.md. Tasks: specs/001-replace-interactions-turns/tasks.md. |
| B-050 | Context-aware actor selection for overflow continue | `implemented` | medium | medium | Replace the simple recency+location sort in `ResolveSceneContinueActorsAsync` with a multi-factor scoring function that accounts for narrative phase, character stats (Desire/Restraint/Dominance), character role, active themes, semantic events, and encounter participation. Includes per-character overrides (auto-participate toggle, priority weight, preferred position). Design: `specs/Planning/rp-context-aware-actor-selection.md`. **[Audit 2026-08-19]** ~45/60 tasks done; LLM actor-selection wired in production. Test coverage + migrations still pending — not `done`. |
| B-051 | Multi-encounter Climax time-skip — split into two-turn instructions | `implemented` | medium | small | Split the combined close-scene + advance-time directive into two sequential instructions across two turns: Turn 1 closes the current encounter naturally; Turn 2 advances time to a new scene. Requires replacing `TimeSkipPending` bool with a `TimeSkipPhase` enum (`None → CloseScene → AdvanceTime`). Preserves existing user-instruction skip and retry semantics for both phases. Plan: `specs/Planning/time-skip-split-instructions.md`. **[Audit 2026-08-19]** `TimeSkipPhase` enum implemented; extended by B-056. |
| B-052 | Full prompt injection refactor — centralize, coordinate, eliminate contradictions | `implemented` | high | large | Current prompt injects (Location Continuity, Escalation Guidance, Scene Deepening Guidance, Scene Pacing Contract, Scene Presence Contract, Intensity Writing Contract, Pacing Directive, Positions list, theme phase guidance) were all added independently with no coordination. No single place defines what injects exist, when each fires, ordering, or conflict resolution. Several injects contradict each other (e.g. removed Scene Presence Contract said "one response = one beat" while fast pacing says "compress multiple beats into one response"). Known conflicts to resolve: (1) Scene Deepening Guidance tells subsequent actors in a turn "do NOT advance to a new beat — deepen the established moment" which directly contradicts fast/medium pacing that wants beat compression; this should be conditional — slow pacing wants deepening, fast pacing should suppress it and let subsequent actors advance too. (2) Escalation Guidance's Climax clauses ("every turn must advance to a new beat") overlap with pacing directives and need reconciliation. (3) Scene Pacing Contract (BuildUp/Reset only) and Pacing Directive (all phases) can co-exist inconsistently. Refactor into a coordinated prompt-builder service or strategy class where each inject is a named component with explicit conditions, ordering, and conflict guards. Audit first — catalog every inject, its conditions, and its exact text — then restructure. **[Audit 2026-08-19]** Coordinated injection implemented, then superseded by the 17-slot `RolePlayPromptBuilder`; injector types deleted. |
| B-053 | Prompt viewer tab on Interaction Info modal | `implemented` | medium | medium | Add a scrollable "LLM Prompt" tab to the Interaction Info modal showing the full prompt text that was sent to the LLM for that interaction. Store the prompt text directly on the `RolePlayInteraction` domain model via a new `PromptText` column. To reduce storage size, include only the first and last sentence of the interaction history block (trimming the middle). The new tab must be scrollable for long prompts. Data populated at prompt-build time when the interaction is created. **[Audit 2026-08-19]** `PromptTextTruncation` + 'LLM Prompt' tab + copy-to-clipboard; `PromptText` populated. |
| B-055 | B-058: Per-encounter memory + knowledge gating | `implemented` | high | large | Plan: specs/Planning/B-058-encounter-memory-knowledge-gating.md **[Audit 2026-08-19]** Duplicate row of B-058 (per-encounter memory + knowledge gating); same feature, implemented. |
| B-056 | Wife-husband aftermath contrast | `done` | high | large | Plan: specs/Planning/B-056-husband-aftermath.md |
| B-057 | Time-skip state race fix | `implemented` | high | medium | Plan: specs/Planning/B-057-time-skip-state-race-plan.md **[Audit 2026-08-19]** Sync persistence + universal encounter tracking landed in code. |
| B-058 | Per-encounter memory + knowledge gating | `implemented` | high | large | Plan: specs/Planning/B-058-encounter-memory-knowledge-gating.md **[Audit 2026-08-19]** `EncounterSummaryRecord`, `GlobalEncounterCount`, universal encounter tracking implemented. |
| B-059 | Semantic encounter-start + memory enrichment contrast | `implemented` | high | medium | Replace keyword-only encounter-start heuristic with LLM semantic inference. Rewrite EncounterCompletion enrichment prompt for vivid first-person prose. Fix displayName data bug. Reset CurrentEncounterStartInteractionIndex after boundary. Branch: 028-encounter-start-detection. Spec: specs/028-encounter-start-detection/spec.md |
| B-065 | Prompt result tester page — debug UI for prompt experimentation | `implemented` | medium | small | V1: page at `/prompt-tester` with model selector, prompt textareas, execute, raw result. V2: persist runs to DB with comment, prompt char count, output word/char count, list/load/delete previous runs. Plan: `specs/065-prompt-tester/plan.md`. |
| B-069 | Story stall fix — HARD CONSTRAINT pacing + context-aware actor selection | `implemented` | high | medium | Implemented: pacing as HARD CONSTRAINT in FinalInstructionSlot, Husband role -5000 score (always last), Wife/OtherMan +100 (lead). Build: 0 errors, 0 warnings. |
| B-071 | BUG: Instructions and character messages count as turns | `done` | high | small | In `RolePlayEngineService.SubmitPromptAsync`, every prompt submission — including `PromptIntent.Instruction` — calls `StartTurnAsync` and increments `ObservedTurnCount++` unconditionally. Instructions (and character messages) should NOT count as narrative turns. Impact: inflates `ObservedTurnCount` (drives Opening→BuildUp at 3 turns), `TurnCountInPhase`, and theme-selection observation windows (`SelectionMinimumTurns`), causing premature phase progression and theme selection. Fix: only start a turn and increment `ObservedTurnCount` for non-instruction intents; guard all `persistedTurn` references (CompleteTurnAsync calls at lines ~1224/1377/1808 and turnIndex usages at ~1673/1762). |
| B-072 | BUG: Instructions disappear / need entering twice | `done` | medium | small | User instructions sometimes disappear from the session or must be entered twice to take effect. Instruction is created as a `System` interaction with `ActorName = "Instruction"` and injected via `UserDirectionSlot` (Slot 16) using `context.PromptText`, but the slot suppresses generic defaults ("continue naturally", "continue", empty) and only fires for the Character variant (not Narrative). Investigate persistence/re-injection of instruction interactions across turns and the `UserDirectionSlot` suppression logic. |
| B-074 | Fix character message & instruction flow — staged + button / … button workflow | `implemented` | high | medium | Rework how character messages and instructions are added and processed. The + button should ADD a character message or instruction to the session but NOT process a continuation; the … button should perform the continuation. The user should be able to add multiple character messages/instructions via +, then use … to run the continuation. Additionally, Pin Interaction should include the pinned character message or instruction into every prompt it is selected for. Covers: (1) split add-vs-continue actions into distinct controls (+ adds, … continues), (2) allow batching multiple added messages/instructions before continuation, (3) ensure pinned character messages/instructions are injected into every prompt of their selected interaction, (4) clarify/align with B-071 (instructions not counting as turns) and B-072 (instructions disappearing). Plan: `specs/Planning/B-074-character-message-instruction-flow.md`. |
| B-076 | Staged Directions slot — one-shot batch queue injected on next continuation | `implemented` | high | medium | Every `+` submission stages with `IsStagedDirection=true` (no toggle). New `StagedDirectionsSlot` (Order 9, after `PinnedContextSlot` Order 8) injects all staged rows as one batch on the next `…` continuation, then graduates them (`IsStagedDirection=false`) before the LLM returns — one-shot even on failure. Staged entries live in `session.Interactions` (flag-based), unlocking all per-interaction commands. `InteractionHistorySlot` filters `!IsStagedDirection`. Full detail: `specs/Planning/B-076-staged-directions-slot.md`. |
| B-077 | Gap-aware steering — willingness-gap aware "Towards" directive | `implemented` | medium | small | Make the steering option generator (UI BuildAllCharacterSteerPromptAsync + background SteerGenerationJobHandler) aware of the Wife willingness gap so TOWARDS/HARD options are geared toward gap-closing content (Loyalty ↓ / Restraint ↓ / SedRecept ↑), role-aware (Husband→neglect, OtherMan→tension spikes, Wife→voiced desire), and with NO direct stat changes (B-020 stays out of scope). Adds shared WillingnessSteerGapResolver (reads RPThemeSemanticStatMappings + ScenarioEngineSettings), a gap-aware context block in both steer prompts, and new WillingnessGapSteeringEnabled/WillingnessGapSteeringDirective fields. Plan: `specs/Planning/B-077-gap-aware-steering.md`. |
| B-078 | OtherMan seduction archetype — role behaviors, character profiles & continuation prompts | `done done` | medium | medium | Define classic seduction tropes (manual labor display, compliments, humor, helping, damsel-in-distress moments, casual physical contact) as first-class role behaviors for the OtherMan. Three layers: (1) SteerRoleIntentCatalog — already updated 2026-08-11 with concrete seduction HOW in TOWARDS direction; (2) character profiles — per-scenario seduction style/traits definition; (3) continuation prompt injection — role-specific seduction guidance in the main narrative generation prompt so the OtherMan seduces competently outside of steering. Distinct from B-077 (gap-aware steering directive) — this is about the fundamental role architecture. |
| B-079 | Attractiveness tier catalog — narrative prose for the 1-10 AttractivenessRating | `done` | medium | medium | Static code-defined catalog mapping the 1-10 AttractivenessRating to effect-based descriptive prose (physical + behavioral cues), rendered by PhysicalAttributesFormatter so the bare number becomes a narrative signal. Bands: 9-10 Striking (turns heads, people fluster, presence commands attention), 7-8 Attractive, 5-6 Average, 3-4 Plain, 1-2 Repelling. Applies to ALL characters and genders (not OtherMan-only) — the same scale renders for the persona, Husband, Wife, and NPCs so attraction is emergent from injected presence, with NO cross-character stat coupling. Integrated into PhysicalAttributesFormatter.FormatBlock (appearance line) and optionally IntimateBehavioralTextBuilder self-awareness prose. Plan: specs/Planning/B-079-attractiveness-tier-catalog.md. |
| B-080 | BUG: OtherMan solo turns — auto-continue generates only the in-scene OtherMan, Wife never reacts | `done` | high | medium | Overflow-continue turns repeatedly generate ONLY Dean (OtherMan) + Narrative; Wife/Husband never react. Root cause: stale location-gate `IsActorInScene` + wrong LLM-detected `CurrentSceneLocation` excluded Becky/Ken, leaving only Dean as in-scene (batchSize=1). Not a seduction-archetype bug. Full detail (4-step verified chain): `specs/Planning/B-080-otherman-solo-turns-bug.md`. |
| B-081 | OtherMan seduction tropes -> semantic stat entries driving Wife Willingness (B-078 follow-up) | `done` | medium | medium | Added 8 `otherman-*` semantic events (one per seduction archetype) with RPSemanticStatMapping rows -> Wife Loyalty down + Restraint down (no Desire, per B-077). Includes semantic-inference prompt-clarity fix (config-backed `RPSemanticEventMapping.Description` for all 165 rows). Full detail: `specs/Planning/B-081-otherman-seduction-semantic-stats.md`. |
| B-082 | Continuation settings popup — pacing, phase guidance markers & word count override | `implemented` | high | medium | Add a button on the Continue As line (alongside You / NPC / Custom / Next Phase) that opens a popup letting the user override continuation settings for the next prompt: pacing (slow / medium / fast), the other phase-guidance markers, and target word count. The chosen values are injected (as marker overrides) into the continuation prompt(s). Each marker must show its human-readable description so users understand what it does. Where feasible, restrict/disable selections that don't make sense for the current context (e.g. phase-incompatible markers, markers that conflict with the current phase). Covers: (1) popup UI entry point on the Continue As line, (2) marker catalog with descriptions, (3) per-continuation override state applied to prompt injection, (4) context-aware validity gating for markers, (5) word-count directive injection, (6) interaction with existing pacing slots (B-068) and FinalInstructionSlot. Plan: `specs/Planning/B-082-continuation-settings-popup.md`. **[Audit 2026-08-19]** `ContinuationOverrideSlot` + popup + 15/15 tests; genuine follow-ups are B-084/B-085. |
| B-087 | Continue — show first returned interaction, don't wait for the full batch | `done` | high | medium | On Continue, the first interaction(s) should be displayed in the workspace as soon as they are returned, instead of waiting for all actors in the batch to complete before showing anything. Improves perceived responsiveness for multi-actor continuations. Covers: (1) streaming/incremental rendering of each completed interaction as it arrives (per-actor chunk delivery already exists via OnRolePlayResponseChunkAsync — extend to full-interaction commit + display), (2) reorder/insertion of completed interactions in place while later actors are still generating, (3) status indication for pending actors, (4) interaction with the read-cursor (B-010) and background submission tracker (B-027). Plan: `specs/Planning/B-087-continue-incremental-display.md`. Implemented: engine `ContinueAsAsync`/`SubmitPromptAsync` gained `onInteractionCompleted` + `onActorStart` callbacks fired per participant + narrative; per-actor DB flush (`QueueRolePlaySessionSave`+`FlushAsync`) inside the batch loop (safe — `AdaptiveState` is `[JsonIgnore]` on the session blob); tracker gained `RolePlayInteractionCallbackWrapper` + `Attach/DetachInteractionCompletedCallback` for B-027 re-attach; workspace `OnRolePlayInteractionCompletedAsync` appends each interaction to `_session.Interactions` + re-renders; `OnRolePlayActorStart` resets the streaming buffer + updates the per-actor pending-bubble label/actor name; B-010 read-cursor pin implemented (scroll-to-cursor when not at bottom, follow-to-bottom when at bottom) on the first completed interaction of the turn; `QueryTrackerOnMountAsync` re-attaches the interaction callback + reloads session on navigation return. `RolePlayPendingResponse` gained `SetActorName`. 9 new tests (callback order, narrative-suppressed, Send parity, null-callbacks, ObjectDisposed-doesn't-abort, tracker attach/detach/dispose-swallow). Build: 0 errors. Tests: 1039 passed. |
| B-088 | BUG: Interaction retry uses character variant, not narrative prompt | `done` | high | small | Retrying an interaction re-generates it as a character interaction instead of replaying the original narrative prompt. When a user clicks retry on an interaction, the retry path should rebuild with the SAME prompt variant (Narrative vs Character) and the original prompt context that produced the interaction — not default to the character variant. Covers: (1) trace the retry code path (Retry / AskToRewrite / regenerate) to confirm which prompt variant and intent it uses, (2) persist/restore the original variant + prompt text on the interaction so retry can faithfully reproduce it, (3) ensure narrative-type interactions retry through ContinueNarrativeAsync / narrative prompt builder rather than the character path, (4) tests covering retry variant fidelity. Related: B-024 (narrative prompt issues), B-053 (prompt viewer — helps verify the retried prompt). Root cause verified: `InteractionRetryService.BuildRetryPromptAsync` builds a standalone character-style rewrite prompt that never consults the stored `PromptText` or any variant signal and bypasses the narrative validation pipeline; `GeneratedByCommand == "Narrative"` reliably identifies narrative interactions in persisted data. Plan: `specs/Planning/B-088-interaction-retry-variant-fidelity.md`. Implemented 2026-08-17: `GeneratedVariant` persisted on interactions; narrative retries route through `ContinueNarrativeAsAlternativeAsync` (narrative builder + validation); `ResolveGeneratedVariant` typed-field → legacy-heuristic; 9 new tests. Build 0 errors; RolePlay 749/749, full suite 1048/1048. Marked `done` 2026-08-18 (user-confirmed functionally stable in dev). |
