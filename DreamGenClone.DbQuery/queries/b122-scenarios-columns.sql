-- The columns of the Scenarios table (one statement), so a follow-up can read the character JSON that carries the
-- gender the studio uses for the body value sets.
SELECT name FROM pragma_table_info('Scenarios');
