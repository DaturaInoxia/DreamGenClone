{
  "name": "dreamgenclone-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E tests for the DreamGenClone Blazor UI (Continuation Settings Tempo/Span popup).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
